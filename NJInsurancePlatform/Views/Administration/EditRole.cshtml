@model NJInsurancePlatform.Models.EditRoleModelView
@{
    ViewData["Title"] = "Edit Role";
}

@*CREATED JUST TO TEST FUNCTIONALITY OF CODE*@


<form method="post" class="mt-3">
    <div class="form-group row">
        <label asp-for="Id" class="col-sm-2 col-form-label"></label>
        <div class="col-sm-10">
            <input asp-for="Id" disabled class="form-control" >
        </div>
        <div class="form-group row">
            <label asp-for="RoleName" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10">
                <input asp-for="RoleName" class="form-control">
                <span asp-validation-for="RoleName" class="text-danger"></span>
            </div>
        </div>
    </div>
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="form-group row">
        <div class="col-sm-10">
            <button type="submit" class="btn btn-primary">Update</button>
            <button class="btn waves-effect waves-light red" asp-action="GetRoles" class="btn btn-primary">Cancel</button>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h3>Users Within This Role</h3>
        </div>
        <div class="card-body">
            @if (Model.Users.Any())
            {
                foreach(var user in Model.Users)
                {
                    <h5 class="card-title">@user</h5>
                }
            }
            else
            {
                <h5 class="card-title">There are no users Assigned To this Role</h5>
            }
        </div>
        <div class="card-footer">
            <a asp-action="EditUsersInRole" asp-controller="Administration" asp-route-roleId="@Model.Id" class="btn btn-primary" style="width:auto;">Add/Remove User</a>

        </div>

    </div>
</form>

@*CREATED JUST TO TEST FUNCTIONALITY OF CODE*@